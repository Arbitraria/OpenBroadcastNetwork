//! Topology management for the overlay network
//!
//! This module has been refactored into a submodule structure for better maintainability.
//! See the `topology/` directory for the implementation details.
//!
//! This file now serves as a re-export layer for backward compatibility.

// Re-export from our new module structure
pub use crate::overlay::topology::geo::{Region, GeoLocation};
pub use crate::overlay::topology::config::TopologyConfig;
pub use crate::overlay::topology::health::ConnectionHealth;
pub use crate::overlay::topology::manager::TopologyManager;

// Re-export for convenience
pub mod geo {
    pub use crate::overlay::topology::geo::*;
}
pub mod config {
    pub use crate::overlay::topology::config::*;
}
pub mod health {
    pub use crate::overlay::topology::health::*;
}
pub mod manager {
    pub use crate::overlay::topology::manager::*;
}
